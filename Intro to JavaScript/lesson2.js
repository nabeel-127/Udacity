function findAverage(x, y) {
	var answer = (x + y) / 2;
	return answer;
  }
  
  var avg = findAverage(5, 10);
  // function returns "Hello!" and console.log prints the return value
  console.log(avg);